type Query {
	getTodos: [Todo]
}

type Mutation {
	createTodo(title: String!): Todo

	deleteTodo(id: String!): String

	toggleTodoCompleted(completed: Boolean!): Boolean
}

type Todo {
	id: String!
	title: String!
	completed: Boolean!
}
